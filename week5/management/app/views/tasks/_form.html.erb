<%= simple_form_for [@task.lecture, @task] do |f| %>
  <%= f.input :name, label: 'Name of task', input_html: {value: @task.name || ''} %>
  <%= f.input :description, as: :text, label: 'What is this task about?', input_html: {value: @task.description || ''} %>
  <%= f.button :submit %>
<% end %>