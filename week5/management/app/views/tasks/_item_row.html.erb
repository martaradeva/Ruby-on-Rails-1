<% t = locals[:task] %>
<tr>
  <td><%= t.name %></td>
  <td><%= t.description %></td>
  <td><%= t.created_at %></td>
  <td><%= t.updated_at %></td>
  <td><%= render 'actions', :locals => {:task => t} %></td>
</tr>